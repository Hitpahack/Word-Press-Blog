@model WP.EDTOs.Categories.CATEGORIES_TERMS_DTO

<li>
    <label for="@Model.Term_Taxonomy_Id">
        <input type="checkbox" checked="@Model.IsChecked" id="@Model.Term_Taxonomy_Id" name="Categories[]" value="@Model.Term_Taxonomy_Id" />
        @Model.Name
    </label>

    @if (Model.Subcategory != null && Model.Subcategory.Any())
    {
        <ul class="children">
            @foreach (var child in Model.Subcategory)
            {
                @Html.Partial("_Categories", child) <!-- Recursive call for child categories -->
            }
        </ul>
    }
</li>
@* <ul>
    @foreach (var category in Model.CategoriesItems)
    {
        <li>
            <label for="@category.Term_Taxonomy_Id">
                <input type="checkbox" checked="@category.IsChecked" id="@category.Term_Taxonomy_Id" name="Categories[]" value="@category.Term_Taxonomy_Id" />
                @category.Name
            </label>

            @if (category.Subcategory != null && category.Subcategory.Any())
            {
                <ul class="children">
                    @foreach (var child in category.Subcategory)
                    {
                        <li>
                            <label for="@child.Term_Taxonomy_Id">
                                <input type="checkbox" checked="@child.IsChecked" id="@child.Term_Taxonomy_Id" name="Categories[]" value="@child.Term_Taxonomy_Id" />
                                @child.Name
                            </label>
                        </li>
                    }
                </ul>
            }
        </li>

    }
</ul> *@