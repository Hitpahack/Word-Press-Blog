<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Yoast SEO Dashboard Clone</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        body {
            background-color: #f8f9fa;
        }

        .sidebar {
            width: 250px;
            background: #fff;
            padding: 20px;
            height: 100vh;
            position: fixed;
        }

        .main-content {
            margin-left: 270px;
            padding: 20px;
        }

        .card {
            border-radius: 10px;
        }

        .chart-container {
            width: 200px;
            height: 200px;
            margin: auto;
        }
    </style>
</head>
<body>
    <div class="d-flex">
        <div class="sidebar">
            <h4 class="text-primary">yoast <span class="text-success">SEO</span></h4>
            <ul class="nav flex-column">
                <li class="nav-item mb-3">
                    <a class="nav-link text-dark fw-bold" href="#">Dashboard</a>
                </li>
                <li class="nav-item mb-3">
                    <a class="nav-link text-dark" href="#">Alert Center</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-dark" href="#">First-time Configuration</a>
                </li>
            </ul>
        </div>
        <div class="main-content">
            <div class="alert alert-light border mb-4">
                <strong>Join our FREE webinar for SEO success</strong>
                <p>Feeling lost when it comes to optimizing your site? Join our FREE webinar...</p>
            </div>
            <h3>Hi benvenuto,</h3>
            <p>Welcome to your dashboard! Check your content’s SEO performance, readability, and more.</p>

            <div class="card p-3 mb-4">
                <h5>SEO Scores</h5>
                <select class="form-select mb-2">
                    <option>Posts</option>
                    <option>Pages</option>
                </select>
                <select class="form-select mb-2">
                    <option>All Categories</option>
                </select>
                <div>
                    <p class="text-success">● Good (1) <button class="btn btn-sm btn-light">View</button></p>
                    <p class="text-warning">● OK (3) <button class="btn btn-sm btn-light">View</button></p>
                    <p class="text-danger">● Needs Improvement (0)</p>
                    <p class="text-secondary">● Not Analyzed (148) <button class="btn btn-sm btn-light">View</button></p>
                </div>
                <div class="chart-container mt-3">
                    <canvas id="seoChart"></canvas>
                </div>
            </div>
        </div>
    </div>

    <script>
        var ctx = document.getElementById('seoChart').getContext('2d');
        var seoChart = new Chart(ctx, {
            type: 'doughnut',
            data: {
                labels: ['Good', 'OK', 'Needs Improvement', 'Not Analyzed'],
                datasets: [{
                    data: [1, 3, 0, 148],
                    backgroundColor: ['#28a745', '#ffc107', '#dc3545', '#6c757d']
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
            }
        });
    </script>
</body>
</html>
